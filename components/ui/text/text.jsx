import { Text as RText } from "react-native";
import tw from "twrnc";


const theme = {
  default: {
    base: ""
  }
}

export const Text = ({variant, className, ...props}) => {

  return (
    <Text style={tw``}>

    </Text>
  )
}